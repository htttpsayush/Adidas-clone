name: Daily Auto Commit

on:
  schedule:
    - cron: '30 2 * * *'
  workflow_dispatch:

jobs:
  auto-commit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Configure Git
        run: |
          git config user.name "htttpsayush"
          git config user.email "ac.ayush2007@gmail.com"

      - name: Auto Commit
        run: |
          echo "Last updated: $(date)" > last_updated.txt
          git add .
          git diff --staged --quiet || git commit -m "auto update: $(date +'%Y-%m-%d')"
          git push
